apiVersion: v1
kind: Service
metadata:
  name: {{ include "blocker-api.fullname" . }}
  labels:
    {{- include "blocker-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blocker-api.selectorLabels" . | nindent 4 }}
