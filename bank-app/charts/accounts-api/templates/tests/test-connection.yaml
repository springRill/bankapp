apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accounts-api.fullname" . }}-test-connection"
  labels:
    {{- include "accounts-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ["--spider", "--server-response", "http://bank-app-accounts-api:8081/actuator/health"]
  restartPolicy: Never
