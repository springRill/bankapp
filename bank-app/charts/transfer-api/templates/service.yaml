apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-api.fullname" . }}
  labels:
    {{- include "transfer-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transfer-api.selectorLabels" . | nindent 4 }}
