apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transfer-api.fullname" . }}-test-connection"
  labels:
    {{- include "transfer-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ["--spider", "--server-response", "http://bank-app-transfer-api:8083/actuator/health"]
  restartPolicy: Never
