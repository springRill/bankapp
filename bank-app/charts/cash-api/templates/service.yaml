apiVersion: v1
kind: Service
metadata:
  name: {{ include "cash-api.fullname" . }}
  labels:
    {{- include "cash-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cash-api.selectorLabels" . | nindent 4 }}
