apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cash-api.fullname" . }}-test-connection"
  labels:
    {{- include "cash-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ["--spider", "--server-response", "http://bank-app-cash-api:8082/actuator/health"]
  restartPolicy: Never
