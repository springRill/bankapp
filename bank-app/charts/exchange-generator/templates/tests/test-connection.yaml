apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "exchange-generator.fullname" . }}-test-connection"
  labels:
    {{- include "exchange-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ["--spider", "--server-response", "http://bank-app-exchange-generator:8085/actuator/health"]
  restartPolicy: Never
