apiVersion: v1
kind: Service
metadata:
  name: {{ include "exchange-api.fullname" . }}
  labels:
    {{- include "exchange-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exchange-api.selectorLabels" . | nindent 4 }}